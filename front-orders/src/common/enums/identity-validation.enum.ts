export enum IdentityValidationEnum {
  INITIAL = 0,
  FAILURE = 1,
  SUCCESS = 2,
  LIMIT_EXCEEDED = 2,
}

export enum FacephiIdentityValidationEnum {
  INITIAL = 0,
  FAILURE = 1,
  SUCCESS = 2,
  LIMIT_EXCEEDED = 2,
}
